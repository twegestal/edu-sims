const attempt = {
    id: String,
    user_id: String,
    case_id: String,
    is_finished: Boolean,
    faults: Number,
    timestamp_started: Date,
    timestamp_finished: Date,
    correct_diagnosis: Boolean
}

