import {
    Modal,
    ModalOverlay,
    ModalContent,
    ModalHeader,
    ModalCloseButton,
    ModalBody,
} from "@chakra-ui/react";

export default function IntroductionModal({ isOpen, onClose, module })  {
    return (
        <p>Introduction</p>
    )
}